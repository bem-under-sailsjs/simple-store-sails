block('users')(
    content()(function() {
            return this.ctx.data.users.map(function(item) {
                return {
                    elem: 'item',
                    item: item,
                    data: this.ctx.data
                };
            }.bind(this));
        }
));
