block('users')(
    content()(function() {
            return this.ctx.data.users.map(function(item) {
                return {
                    elem: 'item',
                    tag: 'li',
                    item: item
                };
            });
        }
));
